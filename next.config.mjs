/** @type {import('next').NextConfig} */
const nextConfig = {
  // site complet static: build-ul (GitHub Actions, la fiecare 20 min)
  // colectează știrile și publică totul pe Cloudflare Pages
  output: 'export',
};

export default nextConfig;
